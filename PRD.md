# 构件生产施工进度管理平台 Web 端 PRD

> 文档版本：V1.0  
> 文档状态：产品方案初稿，可用于原型、UI、数据库与接口设计  
> 更新日期：2026-09-07  
> 产品形态：PC 管理端 + 手机 H5 扫码录入端  
> 暂定产品名：构件云踪（项目启动时可替换）

---

## 1. 文档说明

### 1.1 编写目的

本 PRD 用于明确构件从加工、出厂/进场到现场施工及安装完成全过程的产品需求，作为产品原型、视觉设计、前后端开发、测试验收和项目交付的共同依据。

### 1.2 需求背景

生产制造与施工现场中的构件数量多、流转环节多，当前常见的纸质登记、表格汇总和群聊反馈存在以下问题：

- 构件身份不统一，现场难以快速确认构件信息；
- 加工、出厂、进场、施工和安装信息分散，项目管理人员无法实时掌握全局进度；
- 现场照片与具体构件关联困难，出现质量或进度争议时难以追溯；
- 二维图纸不便于非专业人员识别，缺少直观的三维模型查看能力；
- 人工汇总进度耗时，数据容易重复、遗漏或被覆盖。

平台将为每个构件生成唯一二维码。现场人员用手机扫码即可查看构件并录入进度、节点时间和现场照片；管理人员可在 PC 端同步查看构件台账、统计看板、三维模型和完整操作记录。

### 1.3 本文中的产品假设

以下内容作为 V1.0 默认方案，项目启动前可按实际业务确认：

- 一个二维码唯一对应一个构件，二维码在构件全生命周期内保持不变；
- 对用户展示的主进度为“加工中”“现场施工”“安装完成”三种；
- 新建但尚未产生进度的构件显示为“未录入”，它是系统占位状态，不计入三种业务进度；
- 出厂和进场作为独立里程碑记录，不额外增加主进度状态；
- 手机端优先适配微信内置浏览器、Android Chrome 和 iOS Safari；
- 三维模型 MVP 优先支持 GLB，IFC、RVT、STEP 等工程格式需在上传前或服务端转换；
- 所有扫码录入人员均需登录，默认不开放匿名修改。

---

## 2. 产品目标

### 2.1 业务目标

1. 建立“一构件一码一档案”，统一构件身份与数据入口。
2. 实时同步工厂和施工现场的构件进度，减少人工汇总。
3. 将进度、照片、模型、时间、位置和操作人关联到同一构件。
4. 为项目管理者提供可筛选、可统计、可追溯的项目全景视图。
5. 为后续质量验收、问题闭环、BIM 联动和数据分析预留扩展能力。

### 2.2 成功指标

上线后按项目统计以下指标：

| 指标 | 建议目标 |
| --- | --- |
| 已建档构件二维码覆盖率 | ≥ 98% |
| 现场扫码提交成功率 | ≥ 99%（网络正常时） |
| 进度记录包含操作人和时间的完整率 | 100% |
| 安装完成记录的照片完整率 | ≥ 95%，或按项目配置为 100% |
| PC 端进度更新可见延迟 | ≤ 5 秒 |
| 单个构件查询耗时 | 95% 请求 ≤ 2 秒 |
| 进度数据人工汇总耗时 | 相比原流程降低 ≥ 70% |

---

## 3. 产品范围

### 3.1 MVP（P0）

- 账号登录与角色权限；
- 项目创建、编辑、归档；
- 构件单条创建、批量导入、查询和编辑；
- 一构件一码、二维码批量生成、下载和打印；
- 手机扫码打开构件页面；
- “加工中”“现场施工”“安装完成”进度录入；
- 出厂、进场里程碑时间录入；
- 现场照片拍摄/上传、预览和删除权限控制；
- 构件详情及历史进度时间轴；
- GLB 三维模型上传与网页查看；
- PC 端项目总览、状态统计和构件台账；
- Excel/CSV 导入导出；
- 操作日志与进度回退审批权限；
- 基础弱网保护、重复提交防护和异常提示。

### 3.2 增强功能（P1）

- 项目整体三维模型与构件高亮联动；
- 扫码后定位到整体模型中的对应构件；
- 批次、楼栋、楼层、区域、专业等多维统计；
- 进度逾期预警、消息通知；
- 照片水印（项目、构件、时间、操作人、位置）；
- H5 草稿箱与弱网离线补传；
- 构件问题上报、整改与复检闭环；
- 二维码标签模板自定义；
- SSO/企业微信/钉钉登录。

### 3.3 远期功能（P2）

- IFC 在线解析与 BIM 属性读取；
- 进度计划与实际进度对比、甘特图；
- 物联网设备、RFID 或电子标签接入；
- AI 图片质检或构件识别；
- 多组织、多租户和外部单位协同；
- 原生 App、小程序；
- 与 ERP、MES、WMS、BIM 平台或项目管理系统对接。

### 3.4 本期不做

- 财务结算、采购、库存成本和合同管理；
- 复杂生产排程与设备控制；
- 替代专业 BIM 建模软件；
- 无账号用户直接修改构件数据；
- 对模型源文件进行在线编辑。

---

## 4. 用户角色与权限

| 角色 | 典型用户 | 主要权限 |
| --- | --- | --- |
| 系统管理员 | 企业 IT/平台管理员 | 用户与角色、系统参数、所有项目、日志、存储配置 |
| 项目管理员 | 项目经理/项目负责人 | 管理本项目、导入构件、生成二维码、分配成员、查看全部数据、纠错回退 |
| 工厂录入员 | 加工厂人员 | 查看授权项目构件、录入“加工中”、填写出厂信息、上传照片 |
| 现场录入员 | 施工/安装人员 | 扫码查看构件、填写进场信息、录入“现场施工/安装完成”、上传照片 |
| 查看者 | 甲方、监理、管理层 | 只读查看看板、构件、模型、照片和导出结果 |
| 审计员（可选） | 质量/审计人员 | 只读查看全部记录及审计日志，可导出留档 |

权限规则：

- 权限由“角色权限 + 项目成员范围”共同决定；
- 录入员只能操作被授权项目，不能删除构件或清空历史；
- “安装完成”后的状态回退仅项目管理员可执行，必须填写原因；
- 任何人均不能直接修改历史记录，纠错通过新增更正记录完成；
- 照片删除采用软删除并保留审计信息；
- 默认项目之间数据隔离。

---

## 5. 核心业务对象与术语

| 术语 | 定义 |
| --- | --- |
| 项目 | 构件所属的工程项目，是权限与统计的主要边界 |
| 构件 | 被生产、运输、施工和安装的最小跟踪对象 |
| 构件编码 | 项目内唯一、对业务人员可读的编号 |
| 二维码令牌 | 二维码链接中的随机不可猜测标识，不直接暴露数据库 ID |
| 主进度 | 加工中、现场施工、安装完成三种业务状态 |
| 里程碑 | 出厂、进场等关键事件，记录时间、人员和附件，但不增加主进度枚举 |
| 进度记录 | 每次状态提交产生的不可覆盖历史记录 |
| 模型资源 | 与项目或构件关联的三维文件及其转换结果 |
| 现场照片 | 与某一构件及某次进度记录关联的图片证据 |

---

## 6. 核心流程

### 6.1 管理端建档与发码

1. 项目管理员创建项目并配置项目成员。
2. 管理员单条新增或使用模板批量导入构件。
3. 系统校验项目内构件编码唯一性、必填项及数据格式。
4. 管理员为构件上传模型，或绑定整体模型中的模型元素 ID。
5. 系统为每个构件生成唯一二维码令牌和二维码图片。
6. 管理员按标签模板批量下载 PDF/PNG 并粘贴到实体构件或流转单。

### 6.2 手机扫码录入

1. 录入员使用手机系统相机或微信扫描二维码。
2. 二维码通过 HTTPS 打开 H5 构件页；未登录时先登录，登录成功后自动返回该构件。
3. 系统验证二维码有效性、用户身份及项目权限。
4. 页面显示构件基础信息、当前进度、模型缩略入口和最近记录。
5. 用户选择允许的下一进度，填写里程碑、备注并拍照/上传照片。
6. 用户确认提交；服务端再次校验状态流转，写入进度记录和审计日志。
7. H5 显示提交成功、最新状态和提交时间；PC 端同步刷新。

### 6.3 PC 端查看与追溯

1. 管理人员进入项目总览查看各状态数量、比例和趋势。
2. 通过构件编码、名称、批次、楼栋、楼层、区域、状态等筛选台账。
3. 打开构件详情查看基础资料、三维模型、照片、里程碑和历史时间轴。
4. 对异常数据进行回退或更正，并填写原因。
5. 按当前筛选条件导出报表。

---

## 7. 状态与流转规则

### 7.1 主进度状态机

```text
未录入（系统态） → 加工中 → 现场施工 → 安装完成
                         ↑
                出厂、进场作为里程碑
```

正常情况下只能向前流转，不允许普通录入员跳级或回退。

| 当前状态 | 可提交状态 | 默认执行角色 | 提交要求 |
| --- | --- | --- | --- |
| 未录入 | 加工中 | 工厂录入员、项目管理员 | 可填写加工开始时间；照片默认选填 |
| 加工中 | 现场施工 | 现场录入员、项目管理员 | 建议已填写出厂/进场时间；现场照片至少 1 张 |
| 现场施工 | 安装完成 | 现场录入员、项目管理员 | 完成照片至少 1 张；二次确认 |
| 安装完成 | 无正常下一状态 | 项目管理员可发起回退 | 必须填写回退原因并记录日志 |

### 7.2 状态补充规则

- 管理员可配置“现场施工”和“安装完成”是否强制上传照片，默认均强制；
- 状态时间默认取服务端提交时间；有补录权限的管理员可选择实际发生时间；
- 实际发生时间不得晚于当前时间，且原则上不得早于上一进度时间；
- 重复扫码不会重复创建记录；只有用户明确提交且服务端校验通过才产生记录；
- 提交接口必须支持幂等键，避免弱网重试造成重复数据；
- 若两人同时操作同一构件，后提交者收到“状态已变化”提示并重新确认；
- 已归档项目默认只读，重新开启后才可继续录入；
- 二维码失效或构件被停用时，显示明确原因及联系项目管理员入口。

### 7.3 出厂/进场里程碑

出厂和进场分别记录以下信息：

- 事件类型；
- 实际发生时间；
- 操作人；
- 运输批次/车辆信息（选填）；
- 接收人（进场时选填）；
- 现场位置（按项目配置为选填或必填）；
- 备注；
- 关联照片。

里程碑可以与状态提交一并录入，也可以由授权人员单独补录。

---

## 8. 功能需求

优先级定义：P0 为首版必须，P1 为重要增强，P2 为远期规划。

### 8.1 登录与账号

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| AUTH-01 | 支持手机号/用户名 + 密码登录 | P0 |
| AUTH-02 | 支持退出、会话续期和登录过期处理 | P0 |
| AUTH-03 | 登录前保存二维码目标，登录后自动回跳 | P0 |
| AUTH-04 | 连续失败触发验证码或临时锁定 | P0 |
| AUTH-05 | 支持管理员重置密码、用户首次登录改密 | P0 |
| AUTH-06 | 支持企业微信、钉钉或统一身份认证 | P1 |

### 8.2 项目管理

项目字段包括：项目编码、项目名称、建设单位、施工单位、项目地址、计划开始/结束日期、项目负责人、项目状态、封面、说明。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| PROJ-01 | 创建、编辑、查看、归档项目 | P0 |
| PROJ-02 | 为项目添加成员并指定项目角色 | P0 |
| PROJ-03 | 配置照片必填规则、定位规则、标签模板 | P0/P1 |
| PROJ-04 | 项目归档后二次确认并切换为只读 | P0 |
| PROJ-05 | 展示项目数据完整率与最近更新时间 | P1 |

### 8.3 构件台账

构件基础字段建议如下：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 所属项目 | 是 | 权限与统计边界 |
| 构件编码 | 是 | 项目内唯一，如 GJ-1F-0001 |
| 构件名称 | 是 | 业务名称 |
| 构件类型 | 是 | 梁、柱、墙板、钢构件等，可配置字典 |
| 规格/型号 | 否 | 文本 |
| 材质 | 否 | 文本或字典 |
| 数量/单位 | 否 | 默认 1 件 |
| 批次 | 否 | 用于生产和运输统计 |
| 楼栋/单体 | 否 | 项目空间维度 |
| 楼层 | 否 | 项目空间维度 |
| 区域/轴线 | 否 | 安装位置 |
| 计划加工/进场/安装时间 | 否 | 用于后续预警 |
| 模型元素 ID | 否 | 与整体三维模型中的节点关联 |
| 备注 | 否 | 最多 1000 字 |

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| COMP-01 | 新增、查看、编辑、停用构件 | P0 |
| COMP-02 | 按模板批量导入并提供错误行下载 | P0 |
| COMP-03 | 支持编码、名称、类型、批次、位置、进度等组合筛选 | P0 |
| COMP-04 | 支持分页、排序、列显示设置 | P0 |
| COMP-05 | 按当前筛选条件导出 Excel/CSV | P0 |
| COMP-06 | 批量修改非关键字段，提交前预览影响范围 | P1 |
| COMP-07 | 构件详情集中展示档案、状态、模型、照片和历史 | P0 |
| COMP-08 | 构件不做物理删除；停用时必须填写原因 | P0 |

### 8.4 二维码管理

二维码链接示例：

```text
https://example.com/q/{opaqueToken}
```

其中 `opaqueToken` 为服务端生成的随机令牌，不使用连续数据库 ID，不在二维码中写入构件名称、项目名称等敏感信息。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| QR-01 | 构件创建后自动生成唯一二维码 | P0 |
| QR-02 | 单个下载 PNG/SVG，批量下载标签 PDF | P0 |
| QR-03 | 标签显示项目简称、构件编码、构件名称及二维码 | P0 |
| QR-04 | 支持 A4 排版及常用标签尺寸 | P0 |
| QR-05 | 二维码污损时可重新打印，原二维码继续有效 | P0 |
| QR-06 | 管理员可作废旧令牌并重新生成，旧链接显示已失效 | P0 |
| QR-07 | 记录扫码访问与提交行为，用于审计和统计 | P1 |

### 8.5 手机 H5 扫码录入

H5 首屏必须优先显示：构件编码、名称、所属项目、安装位置、当前进度和醒目的“更新进度”按钮。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| H5-01 | 支持微信/系统相机扫码后直接打开构件页 | P0 |
| H5-02 | 页面适配 360px 及以上常见手机宽度 | P0 |
| H5-03 | 根据角色与当前状态仅显示允许的下一步 | P0 |
| H5-04 | 支持拍照、相册选择、预览、删除后重传 | P0 |
| H5-05 | 上传前压缩大图，保留可审计的原始拍摄时间 | P0 |
| H5-06 | 支持备注、实际发生时间、位置和里程碑填写 | P0 |
| H5-07 | 离开未提交表单前给出提示 | P0 |
| H5-08 | 提交按钮防连点，显示上传进度和明确结果 | P0 |
| H5-09 | 网络中断时保留当前表单并支持重新提交 | P0 |
| H5-10 | 完整离线草稿及恢复联网自动补传 | P1 |

移动端交互要求：

- 单手操作，主要按钮固定在底部安全区域；
- 拍照后先显示本地缩略图，不等待所有图片上传完才允许继续填写；
- 提交过程中不得无提示关闭；
- 图片上传部分失败时展示失败项并支持单独重试；
- 所有错误信息使用业务语言，例如“该构件已由张三更新为现场施工，请刷新后确认”。

### 8.6 进度与时间轴

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| STAT-01 | 每次进度提交生成独立且不可覆盖的历史记录 | P0 |
| STAT-02 | 记录前后状态、操作人、服务端时间、实际时间、备注和附件 | P0 |
| STAT-03 | PC 与 H5 均展示倒序时间轴 | P0 |
| STAT-04 | 管理员可回退状态，必须填写原因并二次确认 | P0 |
| STAT-05 | 纠错与回退均生成审计事件，不修改原记录 | P0 |
| STAT-06 | 支持按时间范围、状态、操作人检索记录 | P1 |

### 8.7 照片与附件

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FILE-01 | 支持 JPG、JPEG、PNG、WebP，单张建议不超过 20 MB | P0 |
| FILE-02 | 单次最多上传 9 张，数量可由管理员配置 | P0 |
| FILE-03 | 存储原图和缩略图，列表优先加载缩略图 | P0 |
| FILE-04 | 图片必须关联构件，可进一步关联具体进度或里程碑 | P0 |
| FILE-05 | 支持大图预览、缩放、下载权限控制 | P0 |
| FILE-06 | 支持按阶段、日期、上传人筛选照片 | P1 |
| FILE-07 | 支持不可篡改样式的项目/构件/时间水印 | P1 |

### 8.8 三维模型

MVP 使用 Three.js 等 WebGL 引擎在线展示 GLB 模型。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| MODEL-01 | 上传构件级 GLB 模型并关联构件 | P0 |
| MODEL-02 | 支持旋转、平移、缩放、复位、全屏和自适应视角 | P0 |
| MODEL-03 | 显示加载进度、文件大小和加载失败原因 | P0 |
| MODEL-04 | 无模型时展示明确占位状态，不阻断进度录入 | P0 |
| MODEL-05 | 上传新版本后保留版本号和历史元数据 | P0 |
| MODEL-06 | 项目整体模型中按状态着色并点击查看构件 | P1 |
| MODEL-07 | 根据模型元素 ID 定位、隔离、高亮对应构件 | P1 |
| MODEL-08 | 支持剖切、测量、构件树和属性面板 | P2 |

模型约束：

- 推荐单个构件 GLB ≤ 50 MB，项目整体模型应进行网格简化、纹理压缩和分块；
- IFC、RVT、STEP、OBJ 等格式不能直接作为 MVP 浏览标准，应先转换为 GLB；
- 模型上传需校验扩展名、MIME、文件头和文件大小；
- 低性能手机默认加载模型缩略图，用户点击后再加载三维资源；
- 模型加载失败不得影响基础信息、照片和进度功能。

### 8.9 项目看板

看板默认展示当前用户有权查看的单个项目；跨项目视图仅管理员可用。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| DASH-01 | 展示构件总数、未录入、加工中、现场施工、安装完成数量 | P0 |
| DASH-02 | 展示各状态占比和总体完成率 | P0 |
| DASH-03 | 点击统计卡片跳转到已带筛选条件的构件台账 | P0 |
| DASH-04 | 展示最近进度动态和最近上传照片 | P0 |
| DASH-05 | 按楼栋、楼层、类型、批次统计 | P1 |
| DASH-06 | 展示计划与实际偏差、逾期构件 | P1 |
| DASH-07 | 支持自动刷新和手动刷新，显示数据更新时间 | P0 |

完成率计算：

```text
完成率 = 安装完成构件数量 ÷ 有效构件总数量 × 100%
```

停用构件不进入分母。所有图表必须同时提供具体数字，不能只用颜色表达状态。

### 8.10 导入导出

- 提供带字段说明、示例行和字典值的标准 Excel 模板；
- 导入采用“上传 → 解析校验 → 错误预览 → 确认入库”流程；
- 同一项目内构件编码重复时默认阻止导入，不静默覆盖；
- 导入失败需返回具体工作表、行号、字段和错误原因；
- 大批量导入导出采用异步任务并展示进度；
- 导出结果遵守当前用户的数据与字段权限；
- 导出文件记录生成者、筛选条件和生成时间。

### 8.11 审计日志

审计范围至少包括：

- 登录成功/失败；
- 项目与成员变更；
- 构件创建、编辑、停用、批量导入；
- 二维码重新生成或作废；
- 进度提交、回退和更正；
- 图片上传、软删除；
- 模型上传和版本替换；
- 敏感导出行为。

每条日志至少包含操作类型、操作者、目标对象、变更前摘要、变更后摘要、服务端时间、IP、设备/浏览器信息。普通用户不可删除审计日志。

---

## 9. 信息架构与页面清单

### 9.1 PC 管理端

```text
登录
└─ 项目选择
   ├─ 项目总览
   ├─ 构件管理
   │  ├─ 构件列表
   │  ├─ 新建/编辑构件
   │  ├─ 批量导入
   │  └─ 构件详情
   │     ├─ 基础信息
   │     ├─ 三维模型
   │     ├─ 现场照片
   │     └─ 进度时间轴
   ├─ 二维码中心
   ├─ 模型中心
   ├─ 进度记录
   ├─ 导入导出任务
   ├─ 项目成员
   └─ 项目设置
系统管理
├─ 用户与角色
├─ 字典配置
├─ 存储/安全参数
└─ 审计日志
```

### 9.2 手机 H5

```text
二维码入口
├─ 登录/身份验证
├─ 构件详情
│  ├─ 基础信息与当前状态
│  ├─ 更新进度
│  ├─ 里程碑录入
│  ├─ 拍照/上传
│  ├─ 模型查看
│  └─ 历史记录
└─ 提交结果
```

### 9.3 PC 构件列表建议列

- 选择框；
- 构件编码；
- 构件名称；
- 类型；
- 批次；
- 楼栋/楼层/区域；
- 当前进度；
- 出厂时间；
- 进场时间；
- 最近更新时间；
- 最近操作人；
- 模型状态；
- 照片数量；
- 操作（详情、二维码、编辑）。

---

## 10. 关键页面低保真说明

### 10.1 项目总览

```text
┌ 项目选择 ─ 日期范围 ─ 楼栋/批次筛选 ─ 刷新 ┐
│ 构件总数 │ 加工中 │ 现场施工 │ 安装完成 │ 完成率 │
├────────────────────────────────────────────┤
│ 状态分布图                  │ 最近进度动态    │
├────────────────────────────────────────────┤
│ 楼栋/楼层进度统计           │ 最近现场照片    │
└────────────────────────────────────────────┘
```

### 10.2 PC 构件详情

```text
┌ 构件编码 / 名称          当前状态        二维码 ┐
├────────────────────────────────────────────┤
│ 三维模型查看器            │ 基础信息/安装位置 │
├────────────────────────────────────────────┤
│ 进度时间轴                │ 现场照片          │
└────────────────────────────────────────────┘
```

### 10.3 手机扫码页

```text
┌─────────────────────┐
│ 项目名称             │
│ GJ-1F-0001  预制柱   │
│ [ 当前：现场施工 ]   │
│ 1#楼 / 1层 / A-03    │
├─────────────────────┤
│ 查看模型  查看照片   │
│ 最近更新：09-07 14:20│
│                     │
│ [ 更新为“安装完成” ] │
└─────────────────────┘
```

---

## 11. 数据模型

### 11.1 实体关系

```text
项目 1 ── N 项目成员 N ── 1 用户
项目 1 ── N 构件
构件 1 ── 1 二维码
构件 1 ── N 进度记录
构件 1 ── N 里程碑记录
构件 1 ── N 照片
构件 1 ── N 模型资源
进度记录 1 ── N 照片
所有关键对象 1 ── N 审计日志
```

### 11.2 核心表建议

| 表 | 关键字段 |
| --- | --- |
| users | id, username, mobile, password_hash, status, created_at |
| roles / permissions | id, code, name；角色权限关联 |
| projects | id, code, name, address, owner, start_date, end_date, status |
| project_members | project_id, user_id, project_role |
| components | id, project_id, code, name, type, spec, batch, building, floor, area, current_status, version, model_element_id |
| qr_codes | id, component_id, token_hash, status, generated_at, revoked_at |
| progress_records | id, component_id, from_status, to_status, actual_at, submitted_at, operator_id, remark, idempotency_key |
| milestones | id, component_id, type, actual_at, operator_id, vehicle_no, receiver, location, remark |
| photos | id, component_id, progress_record_id, milestone_id, object_key, thumbnail_key, mime, size, uploader_id, shot_at, created_at, deleted_at |
| model_assets | id, project_id, component_id, version, format, object_key, size, element_mapping, status, created_at |
| audit_logs | id, actor_id, project_id, action, target_type, target_id, before_json, after_json, ip, user_agent, created_at |

数据约束：

- `components(project_id, code)` 建立唯一索引；
- `qr_codes.token_hash` 建立唯一索引，数据库只保存令牌哈希或受保护值；
- 构件使用 `version` 实现乐观锁，防止并发覆盖；
- `progress_records(component_id, idempotency_key)` 建立唯一约束；
- 所有时间使用 UTC 存储，界面按用户/项目时区显示；
- 业务表保留 `created_at`、`created_by`、`updated_at`、`updated_by`；
- 构件、照片等关键数据采用软删除或停用，不进行无痕物理删除。

---

## 12. 接口清单（建议）

接口统一前缀建议为 `/api/v1`，采用 HTTPS + JSON。文件上传建议使用对象存储预签名地址。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/auth/login` | 登录 |
| POST | `/auth/refresh` | 刷新会话 |
| GET | `/projects` | 获取授权项目 |
| POST | `/projects` | 创建项目 |
| GET | `/projects/{id}/dashboard` | 项目看板统计 |
| GET | `/projects/{id}/components` | 构件列表与筛选 |
| POST | `/projects/{id}/components` | 新增构件 |
| POST | `/projects/{id}/components/import` | 创建导入任务 |
| POST | `/projects/{id}/components/export` | 创建导出任务 |
| GET | `/components/{id}` | 构件详情 |
| PATCH | `/components/{id}` | 编辑构件 |
| GET | `/q/{token}` | 解析二维码并返回受权限控制的构件摘要 |
| POST | `/components/{id}/progress` | 提交主进度 |
| POST | `/components/{id}/milestones` | 提交出厂/进场里程碑 |
| GET | `/components/{id}/timeline` | 获取时间轴 |
| POST | `/components/{id}/status-rollback` | 管理员状态回退 |
| POST | `/files/upload-intents` | 获取上传凭证 |
| POST | `/files/complete` | 确认上传完成并写入元数据 |
| POST | `/components/{id}/models` | 关联模型资源 |
| GET | `/components/{id}/qr-code` | 获取二维码 |
| POST | `/components/qr-codes/batch-export` | 批量生成标签任务 |
| GET | `/audit-logs` | 查询审计日志 |

进度提交请求示例：

```json
{
  "toStatus": "INSTALLATION_COMPLETED",
  "actualAt": "2026-09-07T06:20:00.000Z",
  "remark": "1#楼一层安装完成并自检",
  "photoIds": ["photo_01", "photo_02"],
  "componentVersion": 7,
  "idempotencyKey": "2cc7a44e-60aa-4f77-bf73-2bc87bfc4339"
}
```

统一错误响应至少包含业务错误码、可读提示和请求追踪 ID。前端不得只展示“系统错误”。

---

## 13. 非功能需求

### 13.1 性能

- 默认容量假设：单项目 5 万个构件，企业累计 100 万条进度记录；
- 构件列表、详情和二维码解析接口的 95 分位响应时间 ≤ 2 秒；
- 普通图片上传不占用主业务请求连接，支持断点或失败重试；
- 看板统计结果可短时缓存，但进度变化后应在 5 秒内可见；
- 列表必须服务端分页，禁止一次性加载全量构件；
- 大模型使用压缩、按需加载和 CDN/对象存储分发。

### 13.2 可用性与兼容性

- 月度服务可用性目标 ≥ 99.5%（计划维护除外）；
- PC 支持最新版及前一个大版本的 Chrome、Edge；
- 手机支持主流 Android Chrome、微信内置浏览器、iOS Safari；
- 关键表单内容在刷新或短暂断网时尽可能保留；
- 页面颜色对比度、键盘操作和错误提示满足基础无障碍要求；
- 状态不能仅依赖红黄绿颜色区分，必须同时显示文字或图标。

### 13.3 安全

- 全站 HTTPS，禁止通过 HTTP 提交账号、照片或进度；
- 密码使用 Argon2id 或 bcrypt 等安全算法哈希；
- 登录会话采用短期访问令牌 + 可撤销刷新机制，或安全的服务端 Session；
- Web Cookie 使用 `HttpOnly`、`Secure`、`SameSite`；
- 实施 RBAC 与项目级数据权限，所有后端接口独立鉴权；
- 二维码令牌至少 128 bit 随机熵，可作废、可轮换、不可枚举；
- 文件采用白名单、大小、文件头校验，并预留病毒扫描；
- 对登录、二维码解析、提交和导出接口实施限流；
- 防止 SQL 注入、XSS、CSRF、越权访问和恶意文件上传；
- 对象存储默认私有，访问照片和模型使用短时签名 URL；
- 定期备份数据库，明确恢复点目标与恢复时间目标；
- 精确定位与照片 EXIF 可能涉及隐私，按项目授权采集并配置保留策略。

### 13.4 可维护性与可观测性

- 前后端使用 TypeScript 并启用严格类型检查；
- 关键业务状态使用统一枚举，不在页面中散落硬编码；
- 日志包含请求追踪 ID，避免记录密码、令牌和完整个人敏感信息；
- 监控接口错误率、响应时间、任务积压、存储空间和模型转换失败；
- 数据库变更使用版本化迁移；
- 核心状态流转、权限和二维码解析必须有自动化测试。

---

## 14. 推荐技术方案

以下方案适合在 VS Code 中采用前后端分离方式开发，并非业务需求的唯一实现。

### 14.1 技术选型

| 层级 | 推荐方案 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + TypeScript + Vite | 同一项目提供 PC 与响应式 H5 路由 |
| UI | Element Plus + 自定义移动端组件 | PC 管理端效率高，移动端关键页单独适配 |
| 状态/路由 | Pinia + Vue Router | 权限路由和项目上下文管理 |
| 三维 | Three.js | GLB 加载、控制器、模型高亮 |
| 图表 | ECharts | 状态占比、趋势和楼栋统计 |
| 后端 | NestJS + TypeScript | 模块化、权限守卫、接口校验较成熟 |
| 数据库 | PostgreSQL | 事务、索引、JSON 扩展能力 |
| 缓存/任务 | Redis + BullMQ | 看板缓存、导入导出、标签生成、模型处理 |
| 文件存储 | MinIO 或兼容 S3 的对象存储 | 照片、二维码、报表和模型资源 |
| 部署 | Docker + Nginx | HTTPS、反向代理和静态资源服务 |

如果团队已有 React、Java/Spring Boot 或 .NET 技术栈，可以替换框架，但状态规则、数据模型、安全约束和验收标准应保持一致。

### 14.2 推荐仓库结构

```text
goujiancode/
├─ apps/
│  ├─ web/                 # PC + H5 前端
│  └─ api/                 # 后端 API
├─ packages/
│  ├─ shared-types/        # 状态、DTO、通用类型
│  ├─ ui/                  # 可复用 UI
│  └─ config/              # ESLint/TSConfig 等共享配置
├─ infra/
│  ├─ docker/
│  └─ migrations/
├─ docs/
│  ├─ api/
│  ├─ database/
│  └─ ux/
├─ tests/
└─ PRD.md
```

### 14.3 建议领域模块

- Auth：认证、会话与密码策略；
- IAM：用户、角色、权限和项目成员；
- Project：项目生命周期与配置；
- Component：构件档案、筛选和批量任务；
- Progress：状态机、里程碑、并发与幂等；
- QR：令牌、二维码和标签任务；
- Media：照片上传、缩略图和访问控制；
- Model：三维模型元数据、版本及转换；
- Dashboard：统计与缓存；
- Audit：审计日志与导出记录。

---

## 15. 验收标准

### 15.1 构件与二维码

1. 给定项目管理员导入一份包含 1000 个合法构件的模板，当导入完成时，系统应创建 1000 个构件且构件编码在项目内唯一。
2. 给定模板中存在重复编码，当执行校验时，系统应阻止确认入库，并明确返回重复行号。
3. 给定一个有效构件，当生成二维码并用手机扫描时，应打开该构件页面，且展示的编码与二维码标签一致。
4. 给定已作废二维码，当扫码时，不得显示可提交表单，并应提示二维码已失效。

### 15.2 进度录入

1. 给定状态为“未录入”的构件，工厂录入员只能正常提交为“加工中”。
2. 给定状态为“加工中”的构件，现场录入员可提交为“现场施工”，提交成功后 PC 看板应在 5 秒内反映变化。
3. 给定项目配置“安装完成必须上传照片”，当用户未上传照片时，系统必须阻止提交并提示要求。
4. 给定状态已被他人更新，当旧页面再次提交时，系统不得覆盖新状态，并提示刷新。
5. 给定相同幂等键的重复请求，系统只能创建一条进度记录。
6. 给定“安装完成”构件，普通录入员不得回退；项目管理员回退时必须填写原因。

### 15.3 照片与模型

1. 用户可在手机端拍摄或选择合规图片，看到上传进度并在提交后于 PC 端预览。
2. 不支持的文件类型或超限文件必须在上传前后均被校验，不得写入有效附件。
3. 构件存在合法 GLB 时，PC 端可旋转、平移、缩放、复位和全屏查看。
4. 模型缺失或加载失败时，进度和照片功能仍可正常使用。

### 15.4 权限与审计

1. 用户不得通过修改 URL 查看未授权项目的构件、照片或模型。
2. 查看者不得调用新增、编辑、进度提交、回退或删除接口。
3. 进度提交、回退、二维码作废和导出均能在审计日志中查到操作者、时间和目标对象。
4. 历史进度不允许被无痕修改或物理删除。

### 15.5 兼容性与体验

1. 二维码链接可在微信内置浏览器、Android Chrome 和 iOS Safari 正常打开。
2. 360px 宽度下页面无横向溢出，底部提交按钮不被系统安全区域遮挡。
3. 上传过程中断网后，用户可看到失败原因并在恢复网络后重试，已填写内容不丢失。

---

## 16. 测试重点

- 状态机所有合法/非法流转；
- 普通用户、管理员、查看者和跨项目越权；
- 二维码伪造、枚举、作废、重复扫码；
- 同构件并发更新、弱网重试、重复点击；
- Excel 大批量、重复编码、非法日期、空值和字典异常；
- 多图上传部分失败、超大图片、伪造扩展名；
- 大模型、坏模型、低性能设备和 WebGL 不可用；
- 项目归档后的只读约束；
- 看板数量与构件明细交叉核对；
- 时区、补录时间和跨日统计；
- 备份恢复及对象存储文件可用性。

---

## 17. 迭代建议

### 阶段 A：产品与基础工程

- 确认业务口径、角色、字段和状态规则；
- 完成信息架构、低保真原型和 UI 规范；
- 初始化前后端、数据库、鉴权、部署与自动化检查。

### 阶段 B：MVP 闭环

- 项目、成员、构件台账；
- Excel 导入；
- 二维码生成与手机扫码；
- 三状态进度、里程碑、照片；
- 构件详情、时间轴、基础看板；
- GLB 模型查看；
- 权限、审计和导出。

### 阶段 C：试点优化

- 在一个真实项目进行小批量试点；
- 验证二维码标签耐用性、弱网体验和现场操作时长；
- 优化批量任务、图片压缩、模型性能与统计口径；
- 完成安全、性能、备份恢复与用户验收测试。

### 阶段 D：增强能力

- 整体模型按状态着色；
- 计划偏差与预警；
- 问题整改闭环；
- 企业账号体系及外部业务系统集成。

---

## 18. 风险与应对

| 风险 | 影响 | 应对方案 |
| --- | --- | --- |
| 现场网络不稳定 | 图片和进度提交失败 | 表单本地保留、幂等重试、P1 离线草稿 |
| 二维码磨损或被转发 | 无法识别或越权访问 | 可重复打印、可作废令牌、登录鉴权、项目授权 |
| 模型过大 | 页面卡顿、手机崩溃 | GLB 转换、压缩、分块、延迟加载、缩略图降级 |
| 构件编码不统一 | 导入失败、关联错误 | 项目内唯一约束、标准模板、导入预校验 |
| 多人同时录入 | 状态覆盖或重复记录 | 乐观锁、幂等键、服务端状态机 |
| 现场照片存储增长快 | 成本与加载压力 | 原图/缩略图分层、生命周期策略、项目配额 |
| 状态口径与实际流程不一致 | 统计失真 | 主状态固定、出厂/进场用里程碑、试点后再扩展 |
| 精确位置或人员信息合规 | 隐私风险 | 最小化采集、项目级开关、权限与保留策略 |

---

## 19. 待业务确认项

在 UI 和数据库冻结前，应由业务负责人确认以下问题：

1. 构件主进度是否严格只有三种，是否需要“待加工、已出厂、已进场、验收不合格”等独立状态？
2. 出厂和进场是必须节点，还是只对部分项目/构件启用？
3. 哪些进度必须上传照片，每次最少和最多几张？
4. 是否要求获取手机定位；精度、授权提示和数据保留期限是什么？
5. 是否允许录入员补录实际发生时间，允许补录多少天？
6. 构件数据来源是 Excel、ERP、MES、BIM 模型，还是平台手工建立？
7. 当前模型源格式、单体文件大小、模型元素 ID 规则分别是什么？
8. 二维码粘贴环境、标签材质、尺寸和打印设备是什么？
9. 外部施工单位是否拥有独立账号，是否需要企业微信/钉钉登录？
10. 单项目最大构件数、日均扫码量、并发人数和照片存储周期是多少？
11. 是否需要监理或甲方进行“安装完成”复核后才计入最终完成率？
12. 部署在公网、企业内网还是私有云；手机现场是否能访问该网络？

---

## 20. 产品决策摘要

- 产品采用 PC 管理端 + 手机 H5，用户无需安装 App；
- 二维码是构件入口，不承载敏感业务数据；
- 主进度保持三个，未录入为系统态，出厂/进场采用里程碑；
- 进度历史只追加、不覆盖，所有纠错和回退均可审计；
- 三维 MVP 统一使用 GLB，其他工程格式进入转换流程；
- 照片、状态、操作人和时间必须形成同一条可追溯记录；
- 后端负责最终状态校验、权限、并发和幂等，不能只依赖前端限制；
- 首版先完成“建档—发码—扫码—录入—同步查看—追溯”的闭环，再扩展 BIM 联动、预警和质量管理。

